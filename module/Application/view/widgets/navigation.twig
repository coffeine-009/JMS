<!-- Menu :: Navigation -->
<div class = "menu_navigation_left">
	
	<div class = "label">{{ translate( label ) | raw }}:</div>
	<ul>
		{% for item in items %}
		<li>
			<a href = "{{ item.uri }}">
				{{ translate( item.label ) | raw }}
			</a>
		</li>
		{% endfor %}
	</ul>
	
</div>
